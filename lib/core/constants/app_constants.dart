class AppConstants {
  // App Info
  static const String appName = 'Ensan';
  static const String appVersion = '1.0.0';

  // Animation Durations
  static const Duration splashDuration = Duration(seconds: 3);
  static const Duration heartAnimationDuration = Duration(seconds: 2);
  static const Duration fadeAnimationDuration = Duration(milliseconds: 1500);

  // Text Constants
  static const String appTitle = 'إنسان';
  static const String appSubtitle = 'نبنـــــي جيـــــل...يبنــــي حيــــاة';

  // Validation
  static const int minPasswordLength = 6;
  static const int maxPasswordLength = 20;
}
