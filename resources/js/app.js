/* ============================================================
 * Ensan System — Main JS Entry Point (Vite)
 * يتم استدعاء هذا الملف عبر @vite في الـ layout الرئيسي
 * ============================================================ */

// Bootstrap is loaded once by the main Blade layout so it remains available
// globally to inline page scripts (window.bootstrap).
